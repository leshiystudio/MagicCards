# Repo root is not an Obsidian vault — the vault lives in MagicCards/
/.obsidian/

# Obsidian session/workspace state (local UI state, not vault content)
MagicCards/.obsidian/workspace.json
MagicCards/.obsidian/workspace-mobile.json
